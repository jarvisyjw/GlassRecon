# GlassRecon
Dataset with detailed annotated glass depth maps in indoor scenes.
🚀 The dataset and related code will be released soon~

evaluation
```
python eval.py --image-folder IMAGE_PATH --pred-folder PREDICTED_DEPTH_PATH --sensor-depth-folder SENSOR_DEPTH_PATH --gt-depth-folder GT_DEPTH_PATH --depth-scale DEPTH_SCALE --outdir OUTPUT_PATH (--inverse-depth IF_PREDICTED_DEPTH_IS_INVERSE_DEPTH)
```
